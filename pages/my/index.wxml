<view class="container">
  <view class="main">
    <view class="icon-name">
      <image src="{{userInfo.avatarUrl}}"></image>
      <text>{{userInfo.nickName}}</text>
    </view>
    <view class="wallet" wx:if="{{login}}" bindtap="moveTowallet">
      <text>我的钱包</text>
      <text>></text>
    </view>
  </view>
  <view class="footerwrap"><button type="{{bType}}" bindtap="bindaction">{{activeText}}</button></view>
</view>